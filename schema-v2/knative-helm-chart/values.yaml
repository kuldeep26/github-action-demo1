namespace: "hello-world"

domain:
  name: world.590184049425.realhandsonlabs.net

knativeService:
  name: hello
  image: stacksimplify/kubenginx:1.0.0
  minScale: 1
  serviceEnv:
    SERVICE_URL: "http://syncron.com"

ingress:
  name: test-alb-2
  ingressClass: alb
  scheme: internet-facing
  certificateArn: "" # Populated dynamically via Terraform
  targetService: istio-ingressgateway
  targetPort: 80

aws:
  region: us-east-1

externalSecrets:
  rdsPasswordKey: "" # Populated dynamically via Terraform
